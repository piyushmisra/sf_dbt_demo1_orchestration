name: Deploy Orchestration

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install Snowflake CLI
        uses: Snowflake-Labs/snowflake-cli-action@main
        with:
          cli-version: "latest"

      - name: Execute Orchestration Script
        env:
          # REQUIRED: Tells CLI to look at environment variables
          SNOWFLAKE_CLI_FEATURES_USE_ENV_VARS_FOR_CONNECTION: "true"
          
          # Connection Details (Mapped from your Secrets)
          SNOWFLAKE_ACCOUNT: ${{ secrets.SNOWFLAKE_ACCOUNT }}
          SNOWFLAKE_USER: ${{ secrets.SNOWFLAKE_USER }}
          SNOWFLAKE_PASSWORD: ${{ secrets.SNOWFLAKE_PASSWORD }}
          SNOWFLAKE_ROLE: "ACCOUNTADMIN"
          SNOWFLAKE_WAREHOUSE: "COMPUTE_WH"
        run: |
          # The '-x' flag is the secret sauce: it creates a temporary connection 
          # using only the SNOWFLAKE_... env variables defined above.
          snow sql -f scripts/setup_orchestration.sql -x